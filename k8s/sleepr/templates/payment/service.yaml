apiVersion: v1
kind: Service
metadata:
  labels:
    app: payment
  name: payment
spec:
  ports:
  - name: "tcp"
    port: 3003
    protocol: TCP
    targetPort: 3003
  selector:
    app: payment
  type: ClusterIP
status:
  loadBalancer: {}
