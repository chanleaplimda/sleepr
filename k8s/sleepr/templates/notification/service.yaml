apiVersion: v1
kind: Service
metadata:
  labels:
    app: notification
  name: notification
spec:
  ports:
  - name: "tcp"
    port: 3004
    protocol: TCP
    targetPort: 3004
  selector:
    app: notification
  type: ClusterIP
status:
  loadBalancer: {}
